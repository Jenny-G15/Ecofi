// Importamos las funciones necesarias de la librería React
import React, {createContext, useContext} from "react";

// Creamos un contexto llamado ContextoEcofi
const ContextoEcofi= createContext();

// Exportamos el contexto para que pueda ser utilizado en otros archivos
export default ContextoEcofi
